import 'package:flutter/material.dart';

class AdminMapPage extends StatelessWidget {
  const AdminMapPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Mapa del conductor'),
      ),
    );
  }
}
